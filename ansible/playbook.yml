- name: Deploy to Tomcat
  hosts: web
  become: yes
  tasks:
    - name: Copy WAR file to Tomcat
      copy:
        src: "/devopspractice/MyMavenWebApp04/target/MyMavenWebApp04.war"
        dest: "/opt/tomcat/webapps/MyMavenWebApp04.war"
        remote_src: no

    - name: Restart Tomcat
      systemd:
        name: tomcat
        state: restarted
